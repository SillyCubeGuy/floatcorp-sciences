{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch HTML File",
            "type": "chrome", // For launching in Chrome; can use `pwa-msedge` for Edge
            "request": "launch",
            "file": "${workspaceFolder}/index.html", // Path to your HTML file
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "",
            "postDebugTask": ""
        }
    ]
}
